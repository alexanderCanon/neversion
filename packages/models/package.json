{
  "name": "@neversion/models",
  "version": "1.0.0",
  "description": "Shared TypeScript interfaces for Neversion",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
