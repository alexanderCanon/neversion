# API Contract — Inventory

> [!WARNING]
> **DEPRECATED (Sprint 1.5)**
> The `inventory` concept and the `/api/v1/inventory` endpoints have been removed.
> The Java module `inventory/` no longer exists. No `inventory` table exists in the database.
>
> Pricing metadata is now stored in the `services.details` JSONB column.
>
> **Replacement:** See [services.md](services.md) for the active contract (`/api/v1/services`).
