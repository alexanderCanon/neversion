# Bitácora de Construcción: Panel Administrativo

Este documento es el registro oficial de cambios y decisiones técnicas tomadas durante la implementación del panel administrativo (vendedores y super admin).

## Registro de Cambios

| Fecha | Épica / US | Descripción del Cambio | Decisión Técnica / Nota |
| :--- | :--- | :--- | :--- |
| 2026-04-23 | Setup | Estructura de documentación lista para implementación. | Esperando integración de EPIC-00 en backend. |
| 2026-04-25 | Infra | Fix de dependencias en monorepo. | Se añaden deps faltantes en `packages/utils` y se flexibilizan versiones en `api-client`. |
| 2026-04-27 | EPIC-01 / US-012 | Integración real de Registro de Vendedores. | El front crea el usuario en Supabase y envía el `externalId` al backend. Eliminado flujo de claves temporales. |
| 2026-04-27 | EPIC-01 / US-014 | Lógica de redirección por rol en Login. | Se restringe acceso de clientes al panel administrativo. |
| 2026-04-27 | EPIC-01 / US-015 | Control de acceso por rol. | Rutas protegidas con `roleGuard` (super_admin vs vendedor). |
| 2026-04-27 | EPIC-01 / US-016 | Cierre de sesión seguro. | Implementado logout con Supabase en MainLayout. |
| 2026-04-27 | EPIC-02 / US-017 | Formulario de creación de Servicios. | Implementado con validaciones y nuevos campos de precio/duración alineados con API real. |
| 2026-04-27 | EPIC-02 / US-018 | Edición atómica de Servicios. | Implementado `PUT` hacia la API real con patch de valores. |
| 2026-04-27 | EPIC-02 / US-019 | Activación/Desactivación de Servicios. | Implementado `PATCH /status` para toggle de visibilidad (US-019). |
| 2026-04-27 | EPIC-02 / US-020 | Listado de servicios del vendedor. | Refactorizado para usar `listByVendor` con filtrado por categoría y estado. |
| 2026-04-27 | Lenguaje | Alineación con Glosario B.1. | Renombrado `priceFull` -> `priceComplete`. Labels en UI actualizados a "Cuenta Completa" y "Precio Perfil". |
| 2026-04-27 | EPIC-03 / US-022 | Gestión de Cuentas Maestras. | Formulario actualizado con campos financieros (`cost`, `source`, `purchasedAt`). |
| 2026-04-27 | EPIC-03 / US-025 | Generación Masiva de Perfiles. | Integrado botón de acción rápida para bulk generation desde el detalle de cuenta. |
| 2026-04-27 | EPIC-03 / US-027 | Control Manual de Perfiles. | Implementado bloqueo/desbloqueo quirúrgico con `PATCH /status`. |
| 2026-04-27 | EPIC-03 / US-028 | Detalle de Cuenta (Acordeón). | Implementada carga bajo demanda de perfiles al expandir una cuenta maestra. |
| 2026-04-27 | Infra | Aislamiento Estricto (npmrc). | Configurado `shamefully-hoist=false` para separar físicamente los node_modules de Angular 16 y 17. |
| 2026-04-28 | EPIC-04 / US-029 | Listado de Clientes con Suscripciones | Añadida columna con la cuenta de suscripciones activas (US-029). |
| 2026-04-28 | EPIC-04 / US-030 | Detalle de Cliente Completo | Creación de ClientDetailComponent con Tabs para Info, Suscripciones y Órdenes (US-030). |
| 2026-04-28 | EPIC-04 / US-031 | Creación de Cliente (Validaciones) | Modificado ClientFormComponent para hacer correo requerido y teléfono opcional (US-031). |
| 2026-04-28 | EPIC-04 / US-032 | Edición de Cliente (Email Bloqueado)| Añadida validación de readonly para campo email en modo EDIT (US-032). |
| 2026-04-28 | EPIC-05 / US-035 | Validación de Reservas y Creación de Órdenes. | Implementado flujo de aprobación/rechazo en `ReservationDetailComponent`. |
| 2026-04-28 | EPIC-05 / US-037 | Listado de Órdenes del Vendedor. | Integrado `getOrdersByVendor` con filtrado por estado y búsqueda. |
| 2026-04-28 | EPIC-05 / US-038 | Detalle de Orden con Historial. | Implementado `OrderDetailComponent` incluyendo `statusHistory` y visualización de comprobante. |

| 2026-04-29 | EPIC-06 / US-039 & US-040 | Suggest and Confirm Assignment in OrderDetail. | Calls API to suggest and confirm. Implemented dropdown to override. |
| 2026-04-29 | EPIC-06 / US-030 Fix | Resolved Service and Profile names in ClientDetail. | Updated `ClientService.getDetail()` to perform joins (sub → profile → account → service) for readable names in Panel detail view. |
| 2026-04-29 | EPIC-06 / US-042 | Manual Assignment Modal in Subscriptions. | Opens a modal to select Client, Service, Account and Profile directly. |

| 2026-04-29 | EPIC-07 / US-043 to US-048 | **Full Subscription Life-cycle**: Refactored `SubscriptionsService` to use `SubscriptionsApiService`. Implemented `SubscriptionDetailComponent` (`/subscriptions/:id`) with Renewal (US-045) and Revocation (US-046) capabilities. Updated list view with friendly names and service filtering. Refactored `SubscriptionFormComponent` for manual creation with financial snapshots and notification control (US-048). Added "Detect Expired" manual trigger (US-047). | Full lifecycle management implemented via API-client. Financial data and notifications integrated into manual flows. |
| 2026-04-30 | EPIC-10 / US-063 to US-067 | Implemented vendor KPI dashboard in `apps/panel`: real KPI calls for expiring subscriptions, inventory availability, active clients, successful renewals and gross profit. Dashboard now renders summary cards, expiring subscription groups and inventory by service. | Uses generated `DashboardApiService` after `pnpm api:sync`. Dashboard routes are vendor-only; `super_admin` fallback redirects to `/vendors`. Removed remote Google Fonts import so production build does not depend on network. Verification by Alex: frontend tests/build OK. |
| 2026-05-01 | Architecture / Auth | **Backend-Driven Auth**: Refactored `AuthService` to remove Supabase `signUp()` calls. Form now sends `password` directly to backend via `authApiService.registerVendor`. Added password field to vendor registration form. | Registration is fully backend-managed now. Build tested and passed. |
| 2026-05-05 | QA transversal / Auth & HTTP | Ajustes de calidad durante pruebas integrales del panel: guards restaurados a patrón funcional Angular 17, interceptor HTTP de errores restaurado a `HttpInterceptorFn`, mensajes visibles traducidos a español, acceso a sesión Supabase y log local de errores protegidos para SSR, y eliminación del fallback de rol basado en email. | Los roles del panel ahora dependen solo de metadata de autenticación (`app_metadata` / `user_metadata`). Si el rol no existe, el acceso se trata como no autorizado en lugar de inventar permisos en frontend. |
| 2026-05-05 | QA transversal / Vendor Context | `AuthService` ahora carga `/api/v1/auth/me` después de restaurar sesión o iniciar sesión y expone `currentVendorUuid`. Servicios, cuentas, suscripciones y órdenes usan ese vendor UUID interno para endpoints `vendor/{vendorUuid}` en lugar del UUID externo de Supabase. | Corrige listados vacíos cuando la DB contiene datos bajo `vendors.uuid`. `OrdersService` también corrige su base URL a `/api/v1/orders`. Verificación local: `nvm use 24 && pnpm run build` PASS con warnings preexistentes de budget y selector Bootstrap. |
| 2026-05-05 | QA transversal / Servicios | `ServicesDataService` normaliza la respuesta de `listByVendor1` antes de mapear servicios. | Corrige el error runtime `apiServices.map is not a function` cuando el API devuelve una envoltura con `content` en lugar de un arreglo directo. |
| 2026-05-05 | QA transversal / Servicios | `ServicesDataService` fuerza `Accept: application/json` en las llamadas del cliente generado que esperan respuestas JSON. | Corrige listado vacío causado por `Accept: */*`, que hacía que el cliente generado tratara respuestas JSON como `Blob` y entregara `{}` al pipeline RxJS. |
| 2026-05-05 | EPIC-03 / QA cuentas | `AccountsListComponent` espera a que exista `vendorUuid` antes de disparar la primera carga, separa el estado inicial del vacío real y ofrece reintento ante error. `AccountsService` deduplica peticiones concurrentes por vendor/filtro. | Evita el mensaje falso de "No se encontraron cuentas" al recargar y reduce llamadas redundantes en el arranque sin cambiar el flujo de JWT. |
| 2026-05-05 | EPIC-03 / QA cuentas | `AccountsListComponent` habilita `allowSignalWrites` en el `effect` que dispara la carga inicial. | Corrige `NG0600` al permitir que el flujo reactivo marque estado de carga/error sin bloquear la primera petición. |
| 2026-05-05 | QA transversal / Auth routing | `authGuard` y `guestGuard` ahora leen `AuthService.currentSession()` en vez de volver a llamar `supabase.auth.getSession()`. | Elimina una petición auth redundante por navegación inicial y mantiene el chequeo de sesión alineado con el estado ya resuelto por `APP_INITIALIZER`. |
| 2026-05-05 | EPIC-03 / QA cuentas | `AccountsService` lee `password` del contrato nuevo de `AccountResponse` y mantiene fallback temporal a `pass` hasta regenerar el cliente OpenAPI. | Permite mostrar la contraseña de la cuenta maestra en el listado cuando el backend la expone. Verificación local solicitada: `pnpm build` PASS; no se ejecutaron tests. |
| 2026-05-05 | QA transversal / Resiliencia | `AuthService.initialize()` ya no bloquea el bootstrap si `/api/v1/auth/me` falla. `MainLayoutComponent` muestra un estado de API no disponible con acciones de reintento y salida. | Evita pantalla en blanco cuando el backend está apagado. Verificación local: `pnpm build` PASS; no se ejecutaron tests. |
| 2026-05-05 | EPIC-03 / QA cuentas | El listado de cuentas muestra fecha de renovación y fecha de compra con formato `dd-MM-yyyy`. | Corrige fechas visibles que dependían del formato local por defecto. Verificación local: `pnpm build` PASS; no se ejecutaron tests. |
| 2026-05-05 | EPIC-04 / QA clientes | `ClientsService` usa `/api/v1/clients/vendor/{vendorUuid}` para listar clientes del vendedor y `/api/v1/clients` para creación/edición/detalle. La edición usa `UpdateClientRequest` sin email. | Corrige llamadas erróneas a `/clients` que Spring resolvía como recurso estático y devolvía 500. Verificación local: `pnpm build` PASS; no se ejecutaron tests. |
| 2026-05-05 | EPIC-07 / QA suscripciones | `SubscriptionsService` normaliza la respuesta del listado a `SubscriptionResponse[]`, soporta respuestas envueltas en `content`, fuerza `Accept: application/json` y limpia el signal ante errores. | Corrige el crash Angular `newCollection[Symbol.iterator] is not a function` cuando el `@for` recibía una respuesta no iterable. Verificación local: `pnpm build` PASS; no se ejecutaron tests. |
| 2026-05-05 | EPIC-07 / QA suscripciones | `SubscriptionFormComponent` solicita cuentas maestras ya filtradas por `serviceUuid` al backend y carga perfiles disponibles desde `AccountsService.getAccountDetail(accountId)` al seleccionar cuenta. | Corrige formulario de suscripción manual donde no aparecían cuentas maestras por mezclar UUID público de servicio con `serviceId` interno numérico del listado de cuentas. Verificación local: `pnpm build` PASS; no se ejecutaron tests. |
| 2026-05-05 | EPIC-03 / QA cuentas | `AccountsService` fuerza `Accept: application/json` también en detalle, creación, edición y consulta por id de cuentas. `AccountsListComponent` adapta `ProfileSummaryResponse` al modelo de perfiles que consume la UI y limpia el caché de detalle con actualización inmutable de signal. | Corrige perfiles invisibles al expandir una cuenta maestra. Verificación local: `pnpm build` PASS y Chrome DevTools muestra perfiles renderizados en `/accounts`; no se ejecutaron tests. |
| 2026-05-07 | EPIC-10 / QA dashboard | `MasterDashboardService` fuerza `Accept: application/json` en todas las llamadas al `DashboardApiService` generado mientras el cliente OpenAPI vigente conserva `*/*`. | Corrige el estado falso de error del dashboard cuando los endpoints respondían 200 con JSON pero Angular recibía `Blob`. Verificación local: `pnpm build` PASS y Chrome DevTools muestra KPIs/inventario renderizados en `/dashboard`. |
| 2026-05-07 | EPIC-05 / QA reservas | `ReservationsService` deja de usar `HttpClient` manual contra `/reservations` y consume `ReservationsApiService` generado con `Accept: application/json`. | Corrige el 500 al entrar a Reservas causado por llamar una ruta sin `/api/v1`. Verificación local: `pnpm build` PASS y Chrome DevTools muestra `GET /api/v1/reservations` 200 sin error crítico. |
| 2026-05-07 | EPIC-05 / QA órdenes | `OrdersService` deja de usar `HttpClient` manual y consume `OrdersApiService` generado con `Accept: application/json`. | Mantiene el panel alineado con el cliente OpenAPI y evita parsing como `Blob`. Verificación local: `pnpm build` PASS y Chrome DevTools muestra `GET /api/v1/orders/vendor/{vendorUuid}` 200 sin alerta visible. |
| 2026-05-07 | EPIC-03 / QA cuentas-perfiles | `ProfileListComponent` recibe `canGenerateProfiles` desde el listado de cuentas y oculta la generación manual cuando `saleMode = FULL_ACCOUNT`. | Evita que el panel ofrezca una acción que el backend ahora rechaza por regla de negocio. Verificación local: `pnpm build` PASS con warnings preexistentes de budget y selector Bootstrap. |
| 2026-05-07 | EPIC-07 / QA suscripciones | `SubscriptionDetailComponent` muestra el bloque de acceso de la suscripción: correo, contraseña, perfil y PIN. | Después de `pnpm api:sync`, `access` viene del cliente OpenAPI generado y no requiere tipo local temporal ni override manual de `Accept`. Verificación local: `pnpm build` PASS con warnings preexistentes de budget y selector Bootstrap. |
| 2026-05-07 | QA transversal / OpenAPI JSON contracts | Después de `pnpm api:sync`, los wrappers del panel dejaron de pasar `jsonResponseOptions`; el cliente generado ya declara `httpHeaderAccept?: 'application/json'`. También se retiró el tipo local temporal de `access` en detalle de suscripción. | Cierra la deuda defensiva de `Accept: */*` en Panel. Verificación local: `pnpm build` PASS con warnings preexistentes de budget y selector Bootstrap. |
| 2026-05-11 | QA transversal / Observabilidad super_admin | El rol `super_admin` queda separado del panel operativo de vendedores: las rutas de servicios, cuentas, clientes, suscripciones, reservas, órdenes y dashboard quedan solo para `vendor`. Se agregó `/monitoring` solo para `super_admin`, con link externo seguro hacia Grafana configurado por `GRAFANA_URL`; el sidebar de `super_admin` muestra únicamente Vendedores y Monitoreo. | Evita exposición visual de información privada de vendedores en el panel de super admin. Verificación local: `pnpm build` PASS con warnings preexistentes de budgets y selector Bootstrap. |
| 2026-05-11 | QA transversal / Observabilidad super_admin | Corrección posterior a resolución de conflictos: el sidebar vuelve a envolver Dashboard, Servicios y Cuentas dentro de `@if (!isSuperAdmin())`, y `environment.ts` vuelve a declarar `grafanaUrl` para compilar con `runtimeConfig`. | Verificación local: `pnpm build` PASS con warnings preexistentes de budgets y selector Bootstrap. |
| 2026-05-11 | Infra / Dokploy panel | Se agregó `apps/panel/compose.prod.yml` para desplegar el panel desde Dokploy usando build context en la raíz del monorepo y variables runtime `PANEL_API_URL`, `PANEL_SUPABASE_URL`, `PANEL_SUPABASE_KEY`, `GRAFANA_URL`. | Documentado en `docs/implementation/dokploy-frontend-deploy.md`. |
| 2026-05-11 | Infra / CI panel | `ci-panel.yml` ahora usa pnpm con cache sobre `pnpm-lock.yaml`, instala dependencias desde la raíz del workspace y ejecuta `pnpm --filter panel build`. | Alinea CI con el monorepo pnpm y evita dependencias cacheadas contra lockfiles inexistentes. |
| 2026-05-12 | EPIC-01 / Login UI | Se rediseñó la pantalla de login del panel con layout responsive dividido, bloque de marca Neversion y formulario más pulido con autocompletado y alerta accesible. | Verificación local: `pnpm build` PASS con warnings preexistentes de budgets y selector Bootstrap. |
| 2026-05-12 | Infra / Security hardening | `nginx.conf` del panel ahora agrega headers defensivos (`nosniff`, `DENY`, referrer policy, permissions policy, HSTS y CSP parcial no disruptiva) y los links a comprobantes en pestañas nuevas usan `rel="noopener noreferrer"`. | Verificación local: `pnpm build` PASS con warnings preexistentes de budgets y selector Bootstrap. |
| 2026-05-12 | Infra / Cloudflare Pages panel | El panel queda preparado para Cloudflare Pages con `build:cloudflare`, generación build-time de `assets/runtime-config.js`, `_redirects` para fallback SPA y `_headers` para hardening/cache de runtime config. | Verificación local: `PANEL_API_URL=... PANEL_SUPABASE_URL=... PANEL_SUPABASE_KEY=... GRAFANA_URL=... pnpm run build:cloudflare` PASS con warnings preexistentes de budgets y selector Bootstrap. |
