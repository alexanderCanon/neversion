# Bitácora de Construcción: Backend

Este documento es el registro oficial de cambios y decisiones técnicas tomadas durante la implementación del backend de la plataforma.

## Registro de Cambios

| Fecha | Épica / US | Descripción del Cambio | Decisión Técnica / Nota |
| :--- | :--- | :--- | :--- |
| 2026-04-23 | Setup | Finalización de fase de blindaje de documentación. | Estructura de base de datos y reglas de negocio confirmadas. |
| 2026-04-23 | EPIC-00 / US-001 | V7__create_users_table.sql — tabla `users` con `id` BIGINT IDENTITY, `uuid`, `external_id`, `role` VARCHAR lowercase. | `UserRole` enum, `User` domain model, `UserEntity`, `UserRoleConverter`, `JpaUserAdapter`, `UserPersistenceMapper`, `UserRepositoryPort`. Integration test `UserRepositoryIT` 8/8 PASS. Branch: `feature/backend`. |
| 2026-04-23 | EPIC-00 / US-002 | V8__create_vendors_table.sql — tabla `vendors` con FK a `users`, JSONB `bank_details` y `discount_cfg`. | `Vendor` domain, `VendorEntity` (userId como Long, sin @ManyToOne cross-module), `VendorRepositoryPort`, `JpaVendorAdapter` (bean: `vendorRepositoryAdapter`), `VendorPersistenceMapper`. `VendorRepositoryIT` 8/8 PASS. |
| 2026-04-23 | EPIC-00 / US-003 | V9__link_clients_to_users_vendors.sql — ALTER TABLE `clients` ADD `user_id`, `vendor_id` (nullable). | Update `Client` domain, `ClientEntity`, `ClientPersistenceMapper`. Nuevo `ClientRepositoryIT` 4/4 PASS. FKs nullable para backward compat. |
| 2026-04-23 | EPIC-00 / US-004 | V10__link_services_to_vendors.sql — ALTER TABLE `services` ADD `vendor_id` (nullable). | Update `Service` domain, `ServiceEntity`, `ServicePersistenceMapper`. Tests existentes PASS (backward compat). |
| 2026-04-23 | EPIC-00 / US-005 | V11__normalize_services_pricing.sql — ADD `description`, `image_url`, `price_profile`, `price_full`, `duration_days`, `is_active`. | Update domain + entity + mapper. `@Builder.Default isActive = true` en domain y entity. Tests PASS. |
| 2026-04-23 | EPIC-00 / US-006 | V12__complete_accounts_table.sql — ADD `cost`, `source`, `purchased_at`, `status`, `vendor_id`. | Fix `AccountStatus` (available/partial/full/expired). Update domain + entity + mapper. `@Builder.Default status = AVAILABLE`. Tests PASS. |
| 2026-04-23 | EPIC-00 / US-007 | V13__link_subscriptions_to_vendors.sql — ADD `vendor_id` to subscriptions. | Update domain + entity + mapper. `SubscriptionRepositoryIT` PASS. |
| 2026-04-23 | EPIC-00 / US-008 | V14__normalize_orders_pk_bigint.sql — DROP+recreate orders con BIGINT PK, uuid, vendor_id. | Rewrite completo: domain, entity, port (findByUuid), adapter, mapper, use case, controller, response DTO. ITs PASS. |
| 2026-04-23 | EPIC-00 / US-009+010+011 | V15__normalize_reservations_bigint.sql — DROP+recreate reservations + reservation_details con BIGINT PK, uuid, vendor_id, service_id. | Rewrite completo: 2 domain models, 2 entities, 2 repos, adapter, mapper, port, 3 services, controller, 4 DTOs. Fix 5 test files (4 UT + 1 IT). 23/23 PASS. |
| 2026-04-23 | EPIC-00 / Profiles | V16__link_profiles_to_vendors.sql — ADD `vendor_id` to profiles. | Update domain + entity + mapper. ITs PASS. |
| 2026-04-24 | EPIC-01 / US-012 | V17__create_notification_log_table.sql — tabla `notification_log` con uuid, type, recipient_email, payload, status (pending/sent/failed). | `NotificationLogPort`, `NotificationLogEntity`, `SpringDataNotificationLogRepository`, `JpaNotificationLogAdapter` en `shared`. |
| 2026-04-24 | EPIC-01 / US-012 | Application layer — `RegisterVendorCommand`, `RegisterVendorResult`, `RegisterVendorUseCase` (port in), `RegisterVendorService` en `user/application`. | Orquesta: genera temp password → persiste User(VENDOR) + Vendor → registra VENDOR_WELCOME en notification_log. externalId = placeholder `pending_UUID`. Paso manual Supabase documentado (ADR-09). |
| 2026-04-24 | EPIC-01 / US-012 | REST adapter — `AuthController` + `RegisterVendorRequest` + `RegisterVendorResponse` + `RegisterVendorRequestMapper` + `AuthSecurityConfig` en `auth/infrastructure`. | `POST /api/v1/auth/vendors` protegido con `ROLE_SUPER_ADMIN`. Response incluye `manualStep` con instrucciones de creación en Supabase. |
| 2026-04-24 | EPIC-01 / US-012 | Tests — `RegisterVendorServiceUT` (5 casos, Mockito puro) + `RegisterVendorIT` (8 casos: 401/403/400/201, Nimbus JWT). | Sin dependencias nuevas — JWT firmado con Nimbus JOSE (ya en classpath via oauth2-resource-server). Pendiente: ejecutar y reportar salida. |
| 2026-04-24 | EPIC-01 / US-013 | Application layer — `RegisterClientCommand`, `RegisterClientResult`, `RegisterClientUseCase` (port in), `RegisterClientService` en `user/application`. | Orquesta: resuelve Vendor por UUID (ADR-02) → genera temp password → persiste User(CLIENT) + Client → registra CLIENT_REGISTRATION en notification_log. |
| 2026-04-24 | EPIC-01 / US-013 | REST adapter — `RegisterClientRequest` + `RegisterClientResponse` + `RegisterClientRequestMapper`. Endpoint `POST /api/v1/auth/clients` añadido a `AuthController`. | Endpoint PÚBLICO (permitAll en AuthSecurityConfig). Requiere `vendorUuid` para multi-tenancy. |
| 2026-04-24 | EPIC-01 / US-013 | Tests — `RegisterClientServiceUT` (5 casos) + `RegisterClientIT` (5 casos: 400/201 público). Updated `RegisterVendorIT` con `@MockBean RegisterClientUseCase`. | Pendiente: ejecutar y reportar salida. |
| 2026-04-24 | EPIC-01 / US-014 | **No requiere código backend.** Login gestionado por Supabase Auth (ADR-06/ADR-09). Backend solo valida JWT — ya implementado en `SupabaseJwtAuthConverter` + `SecurityConfig`. Redirección por rol es frontend. | Scope: frontend only. |
| 2026-04-24 | EPIC-01 / US-015 | Refactorización RBAC — 8 SecurityConfigs actualizadas: `hasRole("ADMIN")` → `hasAnyRole("VENDOR", "SUPER_ADMIN")`. Módulos: Account, Dashboard, Order, Profile, Service, Subscription, Reservation, Client. | Alineación con ADR-08 and `UserRole` enum. Zero referencias residuales a `ADMIN`. `ProfileSecurityConfig` cambió `@Component` → `@Configuration`. `ClientSecurityConfig` añadió restricción `GET /clients` (lista) a `VENDOR`+`SUPER_ADMIN`. |
| 2026-04-24 | EPIC-01 / US-016 | **No requiere código backend.** Backend stateless (`SessionCreationPolicy.STATELESS`). Sin sesión server-side. Invalidación es Supabase Auth (revoke token). Token local eliminado por browser. | Scope: frontend only. |
| 2026-04-27 | EPIC-02 / US-017 | `ServiceRequest` enriquecido con campos obligatorios: `category`, `priceProfile`, `priceFull`, `durationDays`, `maxProfiles` + opcionales `description`/`imageUrl`. `ServiceMapper` actualizado. `DigitalServiceService.create()` resuelve `vendorId` del JWT subject (externalId → User → Vendor). | `POST /api/v1/services` requiere `VENDOR`/`SUPER_ADMIN`. |
| 2026-04-27 | EPIC-02 / US-018 | `DigitalServiceService.update()` actualiza todos los campos editables. Verificación de ownership: JWT subject → `UserRepositoryPort.findByExternalId` → `VendorRepositoryPort.findByUserId` → compara `vendorId`. | Ownership fallido lanza `AccessDeniedException` → HTTP 403. |
| 2026-04-27 | EPIC-02 / US-019 | `PATCH /api/v1/services/{id}/status` — toggle `isActive`. Verifica ownership. No afecta suscripciones activas (solo el flag de visibilidad). | `ServiceSecurityConfig` actualizado con regla `PATCH`. |
| 2026-04-27 | EPIC-02 / US-020 | `GET /api/v1/services/vendor/{vendorUuid}?category=&isActive=` — panel del vendedor. `ServiceRepositoryPort` + `SpringDataServiceRepository` con `findAllByVendorId` y `@Query` con filtros opcionales. `ServiceUseCase.listByVendor()`. | Requiere `VENDOR`/`SUPER_ADMIN`. |
| 2026-04-27 | EPIC-02 / US-021 | `GET /api/v1/services/store/{vendorUuid}` — catálogo público, solo activos. `findActiveByVendorId` en port/adapter/repo. | Endpoint público (`permitAll`). |
| 2026-04-27 | EPIC-02 / Infra | `ServiceResponse` enriquecido. `ServiceSecurityConfig` reescrito con RBAC granular. `ServicePersistenceMapper` ya correcto (sin cambios). | |
| 2026-04-27 | EPIC-02 / Tests | `DigitalServiceServiceUT` (11 casos: US-017..021 incluyendo ownership 403). `ServiceControllerIT` (9 casos: RBAC 401/403/400/201, filtros, público). | Pendiente: ejecutar y reportar salida. |
| 2026-04-27 | Contract / US-012 | **Auth refactor (ADR-09 revised):** Elimina `temporaryPassword`. `RegisterVendorRequest` incluye `externalId` obligatorio (Supabase UUID). Response DTOs sin `temporaryPassword`/`manualStep`. | Frontend gestiona creación en Supabase; backend persiste vínculo 1:1. |
| 2026-04-27 | Contract / US-013 | Mismo refactor para clientes. `RegisterClientCommand` añade `externalId`. `RegisterClientService` usa el externalId provisto. | Igual que US-012. |
| 2026-04-27 | Multitenancy | `GET /api/v1/vendors/public/{uuid}` → `VendorPublicResponse(id, storeName, logoUrl)`. `VendorPublicController` + `VendorSecurityConfig` creados (`/public/**` = permitAll). | Storefront resuelve identidad del vendor dinámicamente. |
| 2026-04-27 | DTOs / Consistencia | `OrderResponse.uuid` → `id`. `ReservationResponse.uuid` → `id`. `ReservationDetailResponse.uuid` → `id`. Mappers actualizados. | Estandarización del campo identificador público en todos los módulos. |
| 2026-04-27 | DTOs / Lenguaje ubicuo | `ServiceRequest/Response.priceFull` → `priceComplete`. `ServiceMapper` + `ServiceControllerIT` actualizados. | Alineación con Glosario B.1: "Cuenta Completa". Domain model y columna DB no cambian. |
| 2026-04-27 | EPIC-03 / Fundación | V18__add_status_to_profiles.sql — ADD `status VARCHAR(20) NOT NULL DEFAULT 'available'` a tabla `profiles`. `ProfileStatus` ampliado: AVAILABLE, ACTIVE, RESERVED, OCCUPIED, BLOCKED, EXPIRED. `Profile` domain + `ProfileEntity` + `ProfilePersistenceMapper` + `ProfileResponse` + `ProfileMapper` actualizados. | ACTIVE/RESERVED/OCCUPIED/EXPIRED = solo sistema (EPIC-06/07). AVAILABLE/BLOCKED = manual (US-027). |
| 2026-04-27 | EPIC-03 / US-022 | `AccountRequest` enriquecido: `cost`, `source`, `purchasedAt`, `plan`. `AccountResponse` añade `status`, `saleMode`, `cost`, `source`, `purchasedAt` (password excluido). `AccountMapper` actualizado. `CreateAccountUseCase.create()` → `create(account, callerExternalId)`. `CreateAccountService` resuelve `vendorId` del JWT (ADR-09). `AccountController` extrae `externalId` del `JwtAuthenticationToken`. | `generateProfilesForAccount` ahora recibe `vendorId` para multi-tenancy. |
| 2026-04-27 | EPIC-03 / US-023 | `UpdateAccountUseCase` + `UpdateAccountService` — ownership check (callerVendorId == account.vendorId → 403). `PUT /api/v1/accounts/{id}`. Campos `id`/`uuid` inmutables (BR-US023-01). | |
| 2026-04-27 | EPIC-03 / US-024 | `AccountRepositoryPort` + `SpringDataAccountRepository` + `JpaAccountAdapter` extendidos con `findByVendorId` y `findByVendorIdFiltered(vendorId, serviceId?, status?)`. `ListAccountsUseCase` + `ListAccountsService` nuevos. `GET /api/v1/accounts/vendor/{vendorUuid}?serviceUuid=&status=`. | JPQL con filtros opcionales nullables. |
| 2026-04-27 | EPIC-03 / US-025 | `ProfileUseCase.generate(accountUuid, count, callerExternalId)` + `ProfileService.generate()` — ownership check + validación `existing + count <= service.maxProfiles`. `POST /api/v1/accounts/{id}/profiles/generate?count=N`. | |
| 2026-04-27 | EPIC-03 / US-026 | `ProfileUseCase.update(profileUuid, name, pin, isOwner, callerExternalId)` + `ProfileService.update()` — ownership via `profile → account.vendorId`. `PUT /api/v1/profiles/{id}` actualizado con JWT. | |
| 2026-04-27 | EPIC-03 / US-027 | `ChangeProfileStatusRequest` record. `ProfileUseCase.changeStatus()` + `ProfileService.changeStatus()` — valida que `newStatus ∈ {AVAILABLE, BLOCKED}`, rechaza 400 si sistema-controlado. `PATCH /api/v1/profiles/{id}/status`. | `ProfileSecurityConfig` ya cubre `/**` — no requirió cambio. |
| 2026-04-27 | EPIC-03 / US-028 | `AccountDetailResponse` + `ProfileSummaryResponse` + `AccountSummaryResponse` records. `GetAccountUseCase.getDetail()` + `GetAccountService.getDetail()` — carga profiles, agrupa por status, ownership check. `GET /api/v1/accounts/{id}/detail`. | Compilación limpia `./mvnw compile -q` → exit 0. Pendiente: ejecutar tests. |
| 2026-04-28 | EPIC-04 / US-029 | `ClientRepositoryPort` extendido con `findByVendorId(vendorId, name, phone, email)` + `findByEmail`. `SpringDataClientRepository` con JPQL @Query filtros opcionales nullable. `JpaClientAdapter` implementa ambos. `ClientUseCase.listByVendor()`. `ClientService.listByVendor()` con ownership check (callerExternalId → vendorId). `ClientResponse` añade `activeSubscriptionCount`. `ClientMapper` con overload count. `ClientController` nuevo endpoint `GET /api/v1/clients/vendor/{vendorUuid}?name=&phone=&email=`. | Patrón idéntico a EPIC-03 `listByVendor`. Count calculado en controller via `SubscriptionRepositoryPort.findByClientId`. |
| 2026-04-28 | EPIC-04 / US-030 | `OrderRepositoryPort` + `SpringDataOrderRepository` (native query JOIN reservations) + `JpaOrderAdapter` extendidos con `findByClientId`. `ClientUseCase.getDetail()` + inner records `ClientDetail`, `ActiveSubscriptionSummary`, `OrderSummary`. `ClientService.getDetail()` — ownership check + carga subs activas + historial órdenes. `GET /api/v1/clients/{id}/detail`. | Native query evita cross-module JPA entity references. Inner records en interfaz reducen proliferación de clases DTO. |
| 2026-04-28 | EPIC-04 / US-031 | `ClientRequest` — `email` ahora `@NotBlank @Email`. `ClientRepositoryPort.findByEmail()`. `ClientUseCase.createForVendor()`. `ClientService.createForVendor()` — resuelve vendorId de JWT, valida unicidad email (400), persiste, registra `CLIENT_WELCOME` en `notification_log`. `POST /api/v1/clients` ahora requiere VENDOR JWT. | Patrón notification_log idéntico a EPIC-01. Email inmutable enforced en modelo de request (US-032). |
| 2026-04-28 | EPIC-04 / US-032 | `UpdateClientRequest` [NEW] — sin campo `email` (BR-US032-01). `ClientUseCase.update(uuid, name, phone, notes, callerExternalId)`. `ClientService.update()` — FIX bug: elimina `setEmail()` del update, añade ownership check (403). `PUT /api/v1/clients/{id}` usa `UpdateClientRequest` + JWT. | Bug corregido del scaffolding: update original mutaba email. Nuevo tipo request enforza la regla a nivel contrato. |
| 2026-04-28 | EPIC-04 / Infra | `ClientSecurityConfig` reescrito: todos los endpoints → `hasAnyRole("VENDOR", "SUPER_ADMIN")`. Elimina `permitAll` incorrecto del scaffolding. `ClientService` constructor actualizado con 5 dependencias (+ UserRepo, VendorRepo, SubscriptionRepo, OrderRepo, NotificationLog). | |
| 2026-04-28 | EPIC-04 / Tests | `ClientServiceUT` (14 casos: US-029..032 + legacy). `ClientControllerIT` [NEW] (17 casos: RBAC 401/403/400/201, filtros, detalle). Regresión: `ClientRepositoryIT` 4/4 + `RegisterClientIT` 5/5. Total: 31/31 PASS. | Branch: `feat/epic04`. |
| 2026-04-28 | EPIC-05 / V19-V21 | V19: Enrich orders (clientId, paymentMethod, approvedAt) + reservations (paymentMethod). V20: ADD notes (Text) to reservations. V21: ADD receiptUrl to orders. | Schema normalized to BIGINT PKs. Metadata de-normalization for historical records (US-040). |
| 2026-04-28 | EPIC-05 / US-033 | `CreateReservationService` - calculates total via `ReservationPricingService`, maps clientUuid to clientId, registers `RESERVATION_CREATED`. | Checkout flow entry point. |
| 2026-04-28 | EPIC-05 / US-034 | `UploadReceiptService` - validates status PENDING, updates receiptUrl, transitions to UPLOADED, notifies vendor via `RECEIPT_UPLOADED`. | Ownership check based on reservation vendorId. |
| 2026-04-28 | EPIC-05 / US-035 | `ValidateReservationService` - transitions UPLOADED -> VALIDATED, triggers `CreateOrderService`. Metadata flow: reservation -> order (paymentMethod, receiptUrl). | Ownership check (ADR-02) via JWT callerExternalId. |
| 2026-04-28 | EPIC-05 / US-036 | `RejectReservationService` - requires rejection reason (stored in notes), transitions to REJECTED, notifies client via `RECEIPT_REJECTED`. | Ownership check enforced. |
| 2026-04-28 | EPIC-05 / US-037 | `ListOrdersService` - filtered listing by vendorId, optional clientId and status. `OrderGetController` added `GET /vendor/{uuid}`. | JPQL with optional null parameters. Ownership check 403 if vendor mismatch. |
| 2026-04-28 | EPIC-05 / US-038/39 | `ChangeOrderStatusService` - handles final transitions VALIDATED -> COMPLETED/CANCELLED. Registers notifications `ORDER_COMPLETED`/`ORDER_CANCELLED`. | Prevents modification of already finalized orders. |
| 2026-04-28 | EPIC-05 / Fix | Refactorización de `ReservationItemRequest` para usar `serviceUuid` (UUID) en lugar de `serviceId` (Long). | Permite al frontend crear reservaciones usando identificadores públicos. Sincronizado con `packages/api-client`. |
| 2026-04-28 | EPIC-05 / FIX-1 | **US-035 CA4**: `PAYMENT_APPROVED` notification al cliente. `ValidateReservationService` ahora inyecta `ClientRepositoryPort` + `NotificationLogPort`. Payload incluye `reservationId`, `clientName`, `storeName`, `total`. | Cierra TODO pendiente. |
| 2026-04-28 | EPIC-05 / FIX-2 | **US-037 CA3**: Sort order corregido `DESC → ASC` en `findByVendorIdFiltered` JPQL. FIFO operativo (más antiguas primero). | |
| 2026-04-28 | EPIC-05 / FIX-3 | **US-037 CA2**: `total` + `discount` de-normalizados en Order. V22 migration. `OrderEntity`, `OrderPersistenceMapper`, `CreateOrderService`, `OrderResponse`, `OrderRestMapper` actualizados. | De-normalización justificada: evita JOIN a reservations en listados. |
| 2026-04-28 | EPIC-05 / FIX-4 | **US-038**: `OrderDetailResponse` [NEW] con datos de reservación origen. `OrderGetController.getById()` reescrito: ownership check (403) + carga `Reservation` vía `ReservationRepositoryPort.findById(Long)` [NEW]. `JpaReservationAdapter` implementa `findById`. | Status history pendiente (requiere tabla auditoría — scope EPIC futuro). |
| 2026-04-28 | EPIC-05 / US-038 CA3 | **Historial de estados**: V23 migration `order_status_history` (order_id, old_status, new_status, changed_by, notes, changed_at). `OrderStatusChange` domain, `OrderStatusChangeEntity`, `OrderStatusHistoryPort`, `JpaOrderStatusHistoryAdapter`, `SpringDataOrderStatusChangeRepository`. `CreateOrderService` registra estado inicial. `ChangeOrderStatusService` registra cada transición. `OrderDetailResponse` incluye `statusHistory`. `OrderGetController.getById()` carga historial ASC. | Audit trail completo. |
| 2026-04-28 | EPIC-06 / Foundation | V24 migration agrega `subscriptions.order_id` y `subscriptions.end_date`; `Subscription` domain/entity/mapper/repository expone `findByOrderId`; `SubscriptionResponse` expone `endDate`; `AccountRepositoryPort` agrega `findByServiceIdAndVendorId`. | Decisión técnica aprobada por Alex: `FULL_ACCOUNT` usa el perfil dueño (`isOwner=true`) como ancla de `subscriptions.profile_id`; no se agrega `account_id` a `subscriptions`. |
| 2026-04-28 | EPIC-06 / US-039..042 | Nuevo módulo `assignment`: puertos, DTOs, `SuggestAssignmentService`, `ConfirmAssignmentService`, `DeliverAccessService`, `ManualAssignmentService`, REST `/api/v1/assignments`, security config y UTs. Entrega de accesos usa `notification_log` (`ACCESS_DELIVERED`) post-commit/failure-safe. Alertas sin inventario usan `NO_INVENTORY_ALERT` con routing key `vendor:<id>` y `vendorId` en payload. | `BY_PROFILE` activa solo el perfil seleccionado. `FULL_ACCOUNT` sugiere/usa el perfil dueño, activa todos los perfiles de la cuenta y marca la cuenta `FULL`; EPIC-07 debe liberar perfiles y cuenta al vencer/revocar. Órdenes multi-ítem quedan fuera de alcance y retornan 400 controlado. Verificación local: `./mvnw compile -q` PASS; 19 UTs PASS con `-javaagent` de Byte Buddy. `AssignmentRepositoryIT` agregado pero no ejecutable localmente por Docker/Testcontainers sin acceso al socket. |
| 2026-04-29 | Docs / Saneamiento EPIC-00..11 | Se alinearon historias, reglas de negocio, glosario, ER, gap analysis y análisis de complejidad con el backend real hasta EPIC-06. | Decisiones consolidadas: ADR-09 frontend-managed Supabase Auth; clientes manuales no crean usuario autenticable automático; checkout valida disponibilidad sin bloquear perfiles; órdenes aprobadas nacen `VALIDATED`; rechazo de comprobante usa `REJECTED`; `FULL_ACCOUNT` usa perfil dueño como ancla; `subscriptions.payment_due_date` se conserva y snapshots financieros quedan como decisión de EPIC-07/KPIs. |
| 2026-04-29 | EPIC-06 / US-041 | **Client Access API (Refined)**: Added `GET /api/v1/clients/me/accesses` for secure self-access. New DTO `ClientAccessResponse` exposes credentials (`accountEmail`, `accountPassword`, `profileName`, `profilePin`, etc.). `ClientService.getMyAccesses()` resolves clientId from JWT (ADR-09) to ensure strict ownership. Updated `ClientMapper` for DTO transformation. | Fullstack intervention: secure ID-less endpoint prevents IDOR attacks while delivering credentials to the store. |
| 2026-04-29 | EPIC-07 / US-043 | `ListSubscriptionsUseCase` + `ListSubscriptionsService` implementan listado de suscripciones por vendedor con ownership check (`externalId -> User -> Vendor`). `SubscriptionRepositoryPort`/JPA agrega `findByVendorIdFiltered(vendorId, serviceId?, status?)` con JOIN `subscriptions -> profiles -> accounts` y orden ASC por `payment_due_date`. `GET /api/v1/subscriptions/vendor/{vendorUuid}?serviceId=&status=` retorna cliente, servicio y perfil para el panel. | IDs públicos se mantienen como UUID en el contrato aunque el filtro se llame `serviceId`. Verificación local: `ListSubscriptionsServiceUT` 4/4 PASS usando `-DargLine=-javaagent:...byte-buddy-agent...` por limitación de Mockito inline en WSL/OpenJDK. |
| 2026-04-29 | EPIC-07 / US-044 | V25 agrega snapshots financieros a `subscriptions`: `service_id`, `price_sold`, `discount_applied`, `sale_mode`. `Subscription` domain/entity/mapper actualizados. `GetSubscriptionDetailUseCase` + `GetSubscriptionDetailService` retornan detalle con cliente, perfil, cuenta, servicio, orden origen y snapshot financiero con ownership 403. `GET /api/v1/subscriptions/{id}` ahora retorna `SubscriptionDetailResponse` protegido por JWT. | Se evita reutilizar V24 porque ya pertenece a EPIC-06. Confirmación de asignación con orden ahora captura `serviceId`, `priceSold`, `discountApplied`, `saleMode`; asignación manual EPIC-06 captura `serviceId` y `saleMode`. Verificación local: `GetSubscriptionDetailServiceUT` 3/3 PASS con `-DargLine=-javaagent:...byte-buddy-agent...`. |
| 2026-04-29 | EPIC-07 / US-045 | `SubscriptionRenewalDomainService` implementa BR-07. `RenewSubscriptionUseCase` + `RenewSubscriptionService` renuevan suscripciones `ACTIVE`/`SUSPENDED`, aplican `neversion.renewal.grace-period-days`, incrementan `monthsPaid`, actualizan `paymentDueDate/endDate`, restauran perfil/cuenta (`FULL_ACCOUNT` activa todos los perfiles y cuenta `FULL`) y registran `SUBSCRIPTION_RENEWED` en `notification_log`. `PUT /api/v1/subscriptions/{id}/renew` agregado. | `Clock` inyectable vía `TimeConfig` para pruebas determinísticas. Propiedad de gracia añadida a `application.yaml`, `dev`, `prod` y `test` con default 2. Verificación local: `SubscriptionRenewalDomainServiceUT` 4/4 + `RenewSubscriptionServiceUT` 5/5 PASS con `-DargLine=-javaagent:...byte-buddy-agent...`. |
| 2026-04-29 | EPIC-07 / US-046 | `RevokeSubscriptionUseCase` + `RevokeSubscriptionService` reemplazan cancelación legacy para revocar acceso con ownership check. Cambia suscripción a `CANCELLED`; en `BY_PROFILE` libera el perfil a `AVAILABLE`; en `FULL_ACCOUNT` libera todos los perfiles y la cuenta a `AVAILABLE`; registra `ACCESS_REVOKED`. `PUT /api/v1/subscriptions/{id}/cancel` ahora usa JWT y cascada de inventario. | Verificación local: `RevokeSubscriptionServiceUT` 5/5 PASS con `-DargLine=-javaagent:...byte-buddy-agent...`. |
| 2026-04-29 | EPIC-07 / US-047 | `DetectExpiredSubscriptionsUseCase` + `DetectExpiredSubscriptionsService` suspenden suscripciones activas vencidas (`paymentDueDate <= today`), expiran perfil/cuenta según modalidad y agregan notificación diaria por vendedor `SUBSCRIPTIONS_EXPIRED_DAILY`. `SubscriptionExpiryScheduler` ejecuta a las 02:00 y `POST /api/v1/subscriptions/detect-expired` permite disparo manual. | Endpoint manual restringido a `SUPER_ADMIN`. Config `neversion.cron.subscription-expiry.enabled` añadida en perfiles base/dev/prod/test (test=false). Recipient de vendedor usa routing key `vendor:<id>` por ausencia de email en `User`/`Vendor`, siguiendo patrón EPIC-06. Verificación local: `DetectExpiredSubscriptionsServiceUT` 4/4 PASS con `-DargLine=-javaagent:...byte-buddy-agent...`. |
| 2026-04-29 | EPIC-07 / US-048 | `CreateManualSubscriptionUseCase` + `CreateManualSubscriptionService` crean suscripciones manuales sin orden/reservación previa. Valida ownership de cliente/servicio/perfil, BR-04 overbooking, consistencia perfil-servicio, activa inventario según `BY_PROFILE`/`FULL_ACCOUNT`, guarda snapshots financieros y entrega accesos solo si `sendNotification=true`. `POST /api/v1/subscriptions` ahora usa `CreateManualSubscriptionRequest`. | La modalidad se deriva de la cuenta real del perfil seleccionado para evitar inconsistencias manuales. Verificación local: `CreateManualSubscriptionServiceUT` 4/4 PASS con `-DargLine=-javaagent:...byte-buddy-agent...`. |
| 2026-04-29 | EPIC-07 / Verification | Verificación agregada de EPIC-07 + servicios impactados: 39/39 PASS. Verificación completa de unit tests backend: `./mvnw test -Dtest="*UT" -Dsurefire.failIfNoSpecifiedTests=false -DargLine="-javaagent:/home/alexavers/.m2/repository/net/bytebuddy/byte-buddy-agent/1.17.8/byte-buddy-agent-1.17.8.jar"` → 135/135 PASS. | No se ejecutaron ITs/Testcontainers en este entorno; EPIC-06 ya documentaba limitación local con Docker/Testcontainers. |
| 2026-04-30 | EPIC-08 / Infra (US-049..053) | V26 migration: `notification_log` enriquecido con `entity_type`, `entity_id`, `stage`, `processed_at`, `error_message`. Índice único `idx_notif_dedup(entity_type, entity_id, stage)` para deduplicación. `NotificationLogEntity`, `NotificationLogPort` (6-arg `record()` + `existsByEntityAndStage()`), `JpaNotificationLogAdapter`, `SpringDataNotificationLogRepository` actualizados. | Mantiene backward compatibility: columnas nuevas `nullable`. La 3-arg `record()` legacy conservada como overload delegando a la nueva firma con nulls. |
| 2026-04-30 | EPIC-08 / Email Engine | Integración Resend SDK `4.13.0` + Thymeleaf. `EmailSenderPort` + `ResendEmailAdapter` para envío. `ThymeleafEmailConfig` separado del MVC para resolver plantillas `/templates/email/`. `NotificationWorkerService` procesa batch `pending → sent/failed`. `NotificationScheduler` cada 30s (configurable). `NotificationProcessController`: `POST /process` (SUPER_ADMIN), `GET /health` (público). `NotificationSecurityConfig` con RBAC. | Config requerida: `RESEND_API_KEY`, `NEVERSION_EMAIL_FROM`, `NEVERSION_CRON_NOTIFICATION_WORKER_ENABLED`. Worker es idempotente y failure-safe. |
| 2026-04-30 | EPIC-08 / Templates | `NotificationTemplateResolver` mapea 13 tipos de notificación a plantillas Thymeleaf + subject lines. `TemplateSpec` record con campos `templateName`, `subjectTemplate`, `skipped`. 13 plantillas HTML en `/resources/templates/email/`: bienvenida, registro, pago, acceso, renovación, expiración, recordatorios. | Campo renombrado `skip → skipped` en `TemplateSpec` para evitar colisión con método estático `skip()`. |
| 2026-04-30 | EPIC-08 / Homologación | 12 call-sites actualizados de `record(type, email, payload)` a `record(type, email, payload, entityType, entityId, stage)`: `RegisterVendorService`, `RegisterClientService`, `ClientService`, `ValidateReservationService`, `RejectReservationService`, `UploadReceiptService`, `DeliverAccessService`, `SuggestAssignmentService`, `RevokeSubscriptionService`, `RenewSubscriptionService`, `DetectExpiredSubscriptionsService`, `ChangeOrderStatusService`. 11 archivos de test actualizados para firma de 6 args. | Estrategia de deduplicación: `(entity_type, entity_id, stage)` permite al Worker detectar duplicados y al sistema auditar el ciclo de vida completo de cada entidad. |
| 2026-04-30 | EPIC-08 / US-054 | `SendRenewalRemindersUseCase` + `SendRenewalRemindersService`: escanea suscripciones activas con vencimiento en +7d, +3d, +1d. Deduplicación vía `existsByEntityAndStage()`. `RenewalReminderScheduler` diario a las 08:00 (configurable). `SubscriptionRepositoryPort.findActiveByPaymentDueDate()` + `SpringDataSubscriptionRepository.findByPaymentDueDateAndStatus()` + `JpaSubscriptionAdapter`. `SendRenewalRemindersServiceUT` 5/5 PASS. | Stages: `reminder_7d`, `reminder_3d`, `reminder_1d`. Tipos: `RENEWAL_REMINDER_7D/3D/1D`. Config: `neversion.cron.renewal-reminders.enabled`, `neversion.cron.renewal-reminders.cron`. |
| 2026-04-30 | EPIC-08 / US-055 | `DetectExpiredSubscriptionsService` enriquecido con `ClientRepositoryPort`: por cada suscripción suspendida, registra `SUBSCRIPTION_EXPIRED` al cliente con stage `due`. Errores de notificación no bloquean el flujo principal. `DetectExpiredSubscriptionsServiceUT` actualizado con nuevo constructor (6 deps). | Patrón fire-and-forget: si falla la notificación individual del cliente, el log lo registra y el flujo continúa. Vendor summary (`SUBSCRIPTIONS_EXPIRED_DAILY`) ya existía. |
| 2026-04-30 | EPIC-09 / US-061 | Client-initiated renewal reservations. Added V27 `renewal_subscription_id`, `POST /api/v1/reservations/renew`, `CreateRenewalReservationUseCase`, renewal-aware payment validation, and CLIENT access to `/clients/me/accesses` including `SUSPENDED` subscriptions without credentials. | Client can request renewal, but only vendor approval mutates the subscription. Renewal reservations are deduped while `pending`/`uploaded`; approved renewals create an order, renew the subscription, and complete the order. Verification by Alex: backend tests 254/254 PASS. |
| 2026-04-30 | EPIC-09 / US-059 | Added authenticated client order history via `GET /api/v1/clients/me/orders`. The endpoint resolves the client from JWT, returns only owned orders ordered by `createdAt DESC`, and includes payment metadata plus service lines from the source reservation. | Kept the endpoint under the client module to avoid accepting client IDs in the request and prevent IDOR. Verification by Alex: `ClientServiceUT,ClientControllerIT` 40/40 PASS. |
| 2026-04-30 | EPIC-09 / US-060 | Added authenticated client reservation/receipt status history via `GET /api/v1/clients/me/reservations`. The endpoint resolves the client from JWT, returns owned reservations ordered by `createdAt DESC`, includes receipt status, payment metadata, service lines and rejection notes from `notes`. | Uses a new reservation repository query by internal `clientId`; no client ID is accepted in the request. Verification by Alex: `ClientServiceUT,ClientControllerIT` 44/44 PASS. |
| 2026-04-30 | EPIC-09 / US-062 | Added authenticated client self-profile endpoints: `GET /api/v1/clients/me` and `PUT /api/v1/clients/me`. The update request accepts only `name` and `phone`; `email` remains display-only and immutable. | Self-service profile updates resolve the client from JWT and do not accept client IDs. Verification by Alex: `ClientServiceUT,ClientControllerIT` 53/53 PASS. |
| 2026-04-30 | EPIC-10 / US-063 | Added vendor-only KPI endpoint `GET /api/v1/dashboard/kpis/expiring-subscriptions`. It resolves the vendor from JWT, returns `ACTIVE`/`SUSPENDED` subscriptions due today, tomorrow and later this week, and includes client, service, profile and exact due date. | KPI route is restricted to `VENDOR` only; no vendor ID is accepted in the request. Verification by Alex: `GetExpiringSubscriptionsKpiServiceUT,DashboardControllerIT` 5/5 PASS. |
| 2026-04-30 | EPIC-10 / US-064 | Added vendor-only KPI endpoint `GET /api/v1/dashboard/kpis/inventory-availability`. It resolves the vendor from JWT and returns available/occupied profiles plus available/occupied full accounts grouped by service. | Counts only sellable capacity for the authenticated vendor. Verification by Alex: `GetInventoryAvailabilityKpiServiceUT,GetExpiringSubscriptionsKpiServiceUT,DashboardControllerIT` 10/10 PASS. |
| 2026-04-30 | EPIC-10 / US-065 | Added vendor-only KPI endpoint `GET /api/v1/dashboard/kpis/active-clients`. It resolves the vendor from JWT and returns the count of unique clients with at least one `ACTIVE` subscription. | Counts distinct `subscriptions.client_id` scoped to the authenticated vendor; no vendor ID is accepted in the request. Verification by Alex: `GetActiveClientsKpiServiceUT,GetInventoryAvailabilityKpiServiceUT,GetExpiringSubscriptionsKpiServiceUT,DashboardControllerIT` 15/15 PASS. |
| 2026-04-30 | EPIC-10 / US-066 | Added vendor-only KPI endpoint `GET /api/v1/dashboard/kpis/successful-renewals`. It resolves the vendor from JWT and returns successful renewal orders completed in the current calendar month. | Counts `COMPLETED` orders linked to reservations with `renewal_subscription_id`, filtered by `approved_at` month and authenticated vendor. Verification by Alex: `GetSuccessfulRenewalsKpiServiceUT,GetActiveClientsKpiServiceUT,GetInventoryAvailabilityKpiServiceUT,GetExpiringSubscriptionsKpiServiceUT,DashboardControllerIT` 20/20 PASS. |
| 2026-04-30 | EPIC-10 / US-067 | Added vendor-only KPI endpoint `GET /api/v1/dashboard/kpis/gross-profit`. It resolves the vendor from JWT and returns current-month gross profit in GTQ. | Gross profit uses subscription financial snapshots: subscriptions created in the period plus successful renewal orders in the period, each as `price_sold - discount_applied`. Verification by Alex: `GetGrossProfitKpiServiceUT,GetSuccessfulRenewalsKpiServiceUT,GetActiveClientsKpiServiceUT,GetInventoryAvailabilityKpiServiceUT,GetExpiringSubscriptionsKpiServiceUT,DashboardControllerIT` 25/25 PASS. |
| 2026-05-01 | Architecture / Auth | **Backend-Driven Auth**: `SupabaseAuthPort` added to interact with Supabase Admin API. `RegisterVendorService` and `RegisterClientService` refactored to call `createUser()` and assign roles (`app_metadata.role`) securely. `RegisterVendorRequest` and `RegisterClientRequest` now take `password` instead of `externalId`. | Prevents privilege escalation and isolates user creation to the backend. Tests updated. Backend compiled successfully. |
| 2026-05-05 | QA transversal / Auth Context | `GET /api/v1/auth/me` agregado para exponer contexto autenticado backend-driven: `userUuid`, `externalId`, `role`, `vendorUuid`, `storeName`. `GetCurrentUserContextUseCase` vive en `user/application` y resuelve el vendor interno desde `externalId -> User -> Vendor`. | Cierra la discrepancia entre Supabase UUID y vendor UUID interno sin mover reglas de negocio al frontend. Verificación local: `./mvnw test-compile` PASS; UTs focalizados 18/18 PASS con `-DargLine=-javaagent:/home/alexavers/.m2/repository/net/bytebuddy/byte-buddy-agent/1.17.8/byte-buddy-agent-1.17.8.jar`. |
| 2026-05-05 | QA transversal / Vendor Ownership | `GET /services/vendor/{vendorUuid}` y `GET /accounts/vendor/{vendorUuid}` ahora validan ownership con el JWT del caller antes de listar datos. Los puertos de aplicación reciben `callerExternalId` y las implementaciones comparan el vendor interno del token contra el vendor solicitado. | Mantiene compatibilidad de contrato para el panel, pero elimina exposición cross-vendor por uso directo de `vendorUuid`. Tests unitarios agregados/actualizados para contexto `/me` y ownership de servicios/cuentas. |
| 2026-05-05 | EPIC-03 / QA cuentas | `AccountResponse` ahora expone `password` y `AccountMapper` lo mapea desde la cuenta maestra. | Alineado con el modelo de negocio: la cuenta es la credencial operativa `email + contraseña` que el vendedor administra y entrega. Verificación local solicitada: `./mvnw compile` PASS; no se ejecutaron tests. |
| 2026-05-05 | EPIC-04 / QA clientes | `ClientRepository` dejó de usar JPQL con filtros opcionales `NULL` y ahora lista clientes del vendor mediante `JpaSpecificationExecutor` con predicados dinámicos en `JpaClientAdapter`. | Corrige el 500 de PostgreSQL `function lower(bytea) does not exist` al llamar `GET /api/v1/clients/vendor/{vendorUuid}` sin filtros. Verificación: `./mvnw compile` PASS; `ClientServiceUT,ClientControllerIT,ClientRepositoryIT` 59/59 PASS fuera del sandbox para permitir Mockito agent y Testcontainers/Docker. |
| 2026-05-05 | EPIC-07 / QA suscripciones | `SubscriptionRepository` dejó de usar native SQL con filtros opcionales `NULL` y ahora lista suscripciones por vendedor mediante `JpaSpecificationExecutor` con predicados dinámicos en `JpaSubscriptionAdapter`. El filtro por servicio se resuelve con subquery `profiles -> accounts` sin agregar relaciones JPA innecesarias. | Corrige el 500 de PostgreSQL `could not determine data type of parameter $4` al llamar `GET /api/v1/subscriptions/vendor/{vendorUuid}` sin filtros. Verificación: `./mvnw compile` PASS; `ListSubscriptionsServiceUT,SubscriptionRepositoryIT` 13/13 PASS fuera del sandbox para permitir Mockito agent y Testcontainers/Docker. |
| 2026-05-05 | EPIC-02 / QA servicios | `ServiceRepository` dejó de usar JPQL con filtros opcionales `NULL` y ahora lista servicios por vendedor mediante `JpaSpecificationExecutor` con predicados dinámicos en `JpaServiceAdapter`. | Corrige el 500 de PostgreSQL `could not determine data type of parameter $2` al cargar servicios activos con `category=null` desde pantallas como Suscripciones. Verificación: `./mvnw compile` PASS; `DigitalServiceServiceUT,ServiceRepositoryIT` 21/21 PASS. |
| 2026-05-05 | EPIC-03 / QA cuentas | `AccountRepository` dejó de usar JPQL con filtros opcionales `NULL` y ahora lista cuentas por vendedor mediante `JpaSpecificationExecutor` con predicados dinámicos en `JpaAccountAdapter`. | Corrige el 500 al llamar `GET /api/v1/accounts/vendor/{vendorUuid}?serviceUuid=...` desde el formulario de suscripción manual. Verificación: `./mvnw compile` PASS; `ListAccountsServiceUT,AccountRepositoryIT` 10/10 PASS. |
| 2026-05-07 | EPIC-10 / QA dashboard | `DashboardController` declara `produces = application/json` a nivel de recurso `/api/v1/dashboard`. | Corrige la causa contractual que hacía que el cliente OpenAPI generado usara `Accept: */*` para endpoints JSON del dashboard. Verificación: `./mvnw compile` PASS; `DashboardControllerIT` 15/15 PASS. |
| 2026-05-07 | EPIC-05 / QA reservas | `ReservationController` declara `produces = application/json` a nivel de recurso `/api/v1/reservations`. | Homologa el contrato REST para que el siguiente `api:sync` genere consumo JSON explícito. Verificación: `./mvnw compile` PASS; `ReservationControllerIT` 3/3 PASS. |
| 2026-05-07 | EPIC-05 / QA órdenes | `OrderRepository` dejó de usar JPQL con filtros opcionales `NULL` y ahora lista órdenes por vendedor mediante `JpaSpecificationExecutor` con predicados dinámicos en `JpaOrderAdapter`. `OrderController` y `OrderGetController` declaran `produces = application/json`. | Corrige el 500 al llamar `GET /api/v1/orders/vendor/{vendorUuid}` sin filtros. Verificación: `./mvnw compile` PASS; `ListOrdersServiceUT` 3/3 PASS; Chrome DevTools confirma 200 contra backend dev. |
| 2026-05-07 | EPIC-03 / QA cuentas-perfiles | `ProfileService.generate()` rechaza generación manual para cuentas `FULL_ACCOUNT`; `UpdateAccountService` ya no permite cambiar `saleMode` después de crear la cuenta. | Cierra inconsistencias de inventario donde una cuenta completa podía recibir perfiles extra o cambiar de modalidad con perfiles/suscripciones existentes. Verificación: `./mvnw test -Dtest=UpdateAccountServiceUT,ProfileServiceUT` PASS, 4/4 tests. |
| 2026-05-07 | EPIC-07 / QA suscripciones | `SubscriptionDetailResponse` agrega bloque explícito `access` con `accountEmail`, `accountPassword`, `profileName`, `profilePin` y `saleMode`. | Mantiene `AccountSummary` como resumen operativo y expone las credenciales de la suscripción en un contrato claro para vendedor/cliente. Verificación: `./mvnw test -Dtest=SubscriptionMapperUT,GetSubscriptionDetailServiceUT` PASS, 4/4 tests. |
| 2026-05-07 | QA transversal / OpenAPI JSON contracts | Controllers JSON restantes declaran `produces = application/json`: Auth, Accounts, Services, Profiles, Clients, Subscriptions, Assignments, Notifications y VendorPublic. | Corrección raíz validada con `pnpm api:sync`: el cliente generado dejó de exponer `Accept: */*` para endpoints JSON. Verificación backend: `./mvnw compile` PASS. |
| 2026-05-07 | QA transversal / Exception Handling — Fase 1 | `GlobalHandlerException` enriquecido con dos handlers nuevos: (1) `NoResourceFoundException` → 404 (reemplaza `NoHandlerFoundException` en Spring MVC 6 / Spring Boot 3.x; sin handler caía al catch-all 500). (2) `IllegalStateException` → 401 cuando el mensaje contiene `"JWT principal"` (patrón de todos los helpers `extractExternalId()` en 8 controladores); resto de `IllegalStateException` sigue como 500 con stack trace. Handler `AuthenticationCredentialsNotFoundException` → 401 también agregado por completitud. Imports añadidos: `NoResourceFoundException`, `AuthenticationCredentialsNotFoundException`. | Spring Boot 3.5.10 confirmado. `./mvnw compile -q` exit 0. Fase 2 (semántica 400 vs 409) y Fase 3 (lógica de negocio en ReservationController) pendientes de aprobación de Alex. |
| 2026-05-07 | QA transversal / Exception Handling — Fase 2 | Corrección semántica: guards de estado de recurso que usaban `BadRequestException` (400) migrados a `BusinessRuleException` (409). Archivos: `RenewSubscriptionService` (status != ACTIVE/SUSPENDED → 409), `RevokeSubscriptionService` (status CANCELLED → 409), `CreateManualSubscriptionService` (profile != AVAILABLE → 409, account != AVAILABLE → 409, profiles no todos AVAILABLE → 409). Guards de input del usuario permanecen en 400 (owner profile, due date, perfil no pertenece al servicio). Tests actualizados: `RenewSubscriptionServiceUT`, `RevokeSubscriptionServiceUT`. Verificación: 14/14 PASS. Fase 3 (lógica de negocio en ReservationController.cancelReservation) pendiente. |
| 2026-05-07 | QA transversal / Exception Handling — Fase 3 | Violación de arquitectura hexagonal corregida: lógica de negocio de `cancelReservation` extraída de `ReservationController` hacia `CancelReservationService`. Nuevos archivos: `CancelReservationUseCase` (port in), `CancelReservationService` (application service con `@Transactional`), `CancelReservationServiceUT` (6 casos). Controller reducido a delegación HTTP pura; import `BusinessRuleException` eliminado del controller. OpenAPI `@ApiResponse` de cancelar actualizado a 409 (era 400). Verificación: 9/9 PASS (`CancelReservationServiceUT` + `ReservationControllerIT`). Las 3 fases de Exception Handling QA completadas. |
| 2026-05-07 | QA transversal / Production Readiness — Actuator Security | `/actuator/**` pasó de `permitAll()` a requerir `ROLE_SUPER_ADMIN`. Solo `/actuator/health` permanece público para health probes del load balancer. Test añadido: `actuatorPrometheus_shouldReturn401_withoutToken`. Verificación: 10/10 PASS (`SecurityFilterChainIT`). |
| 2026-05-07 | QA transversal / Production Readiness — A5d Order Tenant Isolation | `GET /orders/by-reservation/{reservationId}` carecía de ownership check. Cualquier usuario autenticado podía ver la orden. Fix: añadido `JwtAuthenticationToken token` al método y llamada a `verifyOwnership()` existente. OpenAPI actualizado a 403. Nota: `verifyOwnership()` lanza 404 si el caller es SUPER_ADMIN (sin registro en vendors) — comportamiento pre-existente en `GET /orders/{id}`, documentado como deuda de Fase B. Verificación: `./mvnw compile` PASS. |
| 2026-05-07 | QA transversal / Production Readiness — A5c Reservation Security | `ReservationSecurityConfig` tenía una regla `permitAll()` para `/reservations/*/guest` que nunca matcheaba (el endpoint real es `/client`). Fix: (1) eliminada la regla huérfana, (2) añadida regla explícita `/client → authenticated()`, (3) añadida regla faltante `/reject → VENDOR|SUPER_ADMIN` que antes caía al catch-all. Las 4 rutas públicas del storefront (create, view, receipt, cancel) confirmadas como intencionales para el flujo de compra anónima. Javadoc actualizado. Verificación: 13/13 PASS (`ReservationControllerIT` + `SecurityFilterChainIT`). |
| 2026-05-07 | QA transversal / Production Readiness — Fase B | B3: `SupabaseJwtAuthConverter` auditado. `exp` validado por NimbusJwtDecoder automáticamente. `iss` sin validación (riesgo teórico bajo con HS256 shared secret). Role null → authorities vacío → 403 en cualquier endpoint protegido (correcto). Javadoc corregido: `raw_app_meta_data` → `app_metadata`, eliminada referencia al fallback a `user_metadata` que nunca existió en código. Deuda B: `verifyOwnership()` en `OrderGetController` refactorizado — ya no lanza 404 para SUPER_ADMIN sin vendor profile; retorna sin error (bypass por ausencia de vendor profile, justificado por la capa de seguridad). Import huérfano eliminado. Verificación: `./mvnw compile` PASS. |
| 2026-05-07 | QA transversal / Production Readiness — Fase C Profile Tests | `ProfileServiceFullUT` creado con 15 casos: US-025 generate (4 casos: FULL_ACCOUNT→409, exceedsMax→409, 403 cross-vendor, happy path), US-026 update (4 casos: 404, 403 cross-vendor, patch name, patch pin), US-027 changeStatus (5 casos: ACTIVE→409, RESERVED→409, 404, BLOCKED ok, AVAILABLE ok), deleteById (2 casos: 404, happy path). Verificación: 15/15 PASS. |
| 2026-05-07 | QA transversal / Production Readiness — Fase D Observabilidad | `application-prod.yaml` corregido: (1) `flyway.baseline-on-migrate` → false (previene saltar migraciones pendientes en DB existente), (2) añadido `server.shutdown: graceful` + `lifecycle.timeout-per-shutdown-phase: 30s` (drain in-flight requests), (3) `server.forward-headers-strategy: native` (soporte correcto de reverse proxy). HikariCP ya estaba configurado (pool 10, idle 2, timeouts). Logging ya en WARN para root. |
| 2026-05-11 | QA transversal / Observabilidad super_admin | `/actuator/prometheus` ahora acepta autenticación de servicio mediante `NEVERSION_MONITORING_SCRAPE_TOKEN` para scrape de Prometheus sin depender de JWT humano. Se agregaron `MonitoringScrapeTokenAuthenticationFilter` y `MonitoringAwareBearerTokenResolver`; `/actuator/health` permanece público y `/actuator/**` sigue protegido para `SUPER_ADMIN`. Se agregó `apps/api/monitoring/prometheus.compose.yml` para desplegar Prometheus en Dokploy desde el repo usando variables de entorno, y se documentó el despliegue en `apps/api/monitoring/README.md`. | Verificación: `./mvnw compile` PASS; `MonitoringAwareBearerTokenResolverUT,MonitoringScrapeTokenAuthenticationFilterUT` 6/6 PASS; `SecurityFilterChainIT` 12/12 PASS confirmado por Alex. |
| 2026-05-11 | Infra / Red interna observabilidad | `compose.prod.yml` del API y `prometheus.compose.yml` se conectan a una red Docker externa compartida `neversion-internal`. El API publica el alias interno `neversion-api`; Prometheus usa `NEVERSION_API_TARGET=neversion-api:8080` por defecto. | Alex creara la red una vez en el servidor con `docker network create neversion-internal`. Esto permite scrape interno sin depender del dominio publico del API. |
| 2026-05-11 | Infra / Grafana Dokploy | Se agregó `apps/api/monitoring/grafana.compose.yml` para desplegar Grafana como app separada en Dokploy. Grafana queda conectado a `neversion-internal`, usa datasource provisionado `http://prometheus:9090`, desactiva acceso anónimo y recibe `GRAFANA_URL`, `GF_SECURITY_ADMIN_USER`, `GF_SECURITY_ADMIN_PASSWORD` por variables de entorno. Prometheus publica alias interno `prometheus`. | Permite que el panel `super_admin` abra Grafana por link externo seguro mientras Grafana consulta Prometheus por red interna. |
