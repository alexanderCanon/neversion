# Bitácora de Construcción: Backend

Este documento es el registro oficial de cambios y decisiones técnicas tomadas durante la implementación del backend de la plataforma.

## Registro de Cambios

| Fecha | Épica / US | Descripción del Cambio | Decisión Técnica / Nota |
| :--- | :--- | :--- | :--- |
| 2026-04-23 | Setup | Finalización de fase de blindaje de documentación. | Estructura de base de datos y reglas de negocio confirmadas. |
| 2026-04-23 | EPIC-00 / US-001 | V7__create_users_table.sql — tabla `users` con `id` BIGINT IDENTITY, `uuid`, `external_id`, `role` VARCHAR lowercase. | `UserRole` enum, `User` domain model, `UserEntity`, `UserRoleConverter`, `JpaUserAdapter`, `UserPersistenceMapper`, `UserRepositoryPort`. Integration test `UserRepositoryIT` 8/8 PASS. Branch: `feature/backend`. |
| 2026-04-23 | EPIC-00 / US-002 | V8__create_vendors_table.sql — tabla `vendors` con FK a `users`, JSONB `bank_details` y `discount_cfg`. | `Vendor` domain, `VendorEntity` (userId como Long, sin @ManyToOne cross-module), `VendorRepositoryPort`, `JpaVendorAdapter` (bean: `vendorRepositoryAdapter`), `VendorPersistenceMapper`. `VendorRepositoryIT` 8/8 PASS. |
| 2026-04-23 | EPIC-00 / US-003 | V9__link_clients_to_users_vendors.sql — ALTER TABLE `clients` ADD `user_id`, `vendor_id` (nullable). | Update `Client` domain, `ClientEntity`, `ClientPersistenceMapper`. Nuevo `ClientRepositoryIT` 4/4 PASS. FKs nullable para backward compat. |
| 2026-04-23 | EPIC-00 / US-004 | V10__link_services_to_vendors.sql — ALTER TABLE `services` ADD `vendor_id` (nullable). | Update `Service` domain, `ServiceEntity`, `ServicePersistenceMapper`. Tests existentes PASS (backward compat). |
| 2026-04-23 | EPIC-00 / US-005 | V11__normalize_services_pricing.sql — ADD `description`, `image_url`, `price_profile`, `price_full`, `duration_days`, `is_active`. | Update domain + entity + mapper. `@Builder.Default isActive = true` en domain y entity. Tests PASS. |
| 2026-04-23 | EPIC-00 / US-006 | V12__complete_accounts_table.sql — ADD `cost`, `source`, `purchased_at`, `status`, `vendor_id`. | Fix `AccountStatus` (available/partial/full/expired). Update domain + entity + mapper. `@Builder.Default status = AVAILABLE`. Tests PASS. |
| 2026-04-23 | EPIC-00 / US-007 | V13__link_subscriptions_to_vendors.sql — ADD `vendor_id` to subscriptions. | Update domain + entity + mapper. `SubscriptionRepositoryIT` PASS. |
| 2026-04-23 | EPIC-00 / US-008 | V14__normalize_orders_pk_bigint.sql — DROP+recreate orders con BIGINT PK, uuid, vendor_id. | Rewrite completo: domain, entity, port (findByUuid), adapter, mapper, use case, controller, response DTO. ITs PASS. |
| 2026-04-23 | EPIC-00 / US-009+010+011 | V15__normalize_reservations_bigint.sql — DROP+recreate reservations + reservation_details con BIGINT PK, uuid, vendor_id, service_id. | Rewrite completo: 2 domain models, 2 entities, 2 repos, adapter, mapper, port, 3 services, controller, 4 DTOs. Fix 5 test files (4 UT + 1 IT). 23/23 PASS. |
| 2026-04-23 | EPIC-00 / Profiles | V16__link_profiles_to_vendors.sql — ADD `vendor_id` to profiles. | Update domain + entity + mapper. ITs PASS. |
| 2026-04-24 | EPIC-01 / US-012 | V17__create_notification_log_table.sql — tabla `notification_log` con uuid, type, recipient_email, payload, status (pending/sent/failed). | `NotificationLogPort`, `NotificationLogEntity`, `SpringDataNotificationLogRepository`, `JpaNotificationLogAdapter` en `shared`. |
| 2026-04-24 | EPIC-01 / US-012 | Application layer — `RegisterVendorCommand`, `RegisterVendorResult`, `RegisterVendorUseCase` (port in), `RegisterVendorService` en `user/application`. | Orquesta: genera temp password → persiste User(VENDOR) + Vendor → registra VENDOR_WELCOME en notification_log. externalId = placeholder `pending_UUID`. Paso manual Supabase documentado (ADR-09). |
| 2026-04-24 | EPIC-01 / US-012 | REST adapter — `AuthController` + `RegisterVendorRequest` + `RegisterVendorResponse` + `RegisterVendorRequestMapper` + `AuthSecurityConfig` en `auth/infrastructure`. | `POST /api/v1/auth/vendors` protegido con `ROLE_SUPER_ADMIN`. Response incluye `manualStep` con instrucciones de creación en Supabase. |
| 2026-04-24 | EPIC-01 / US-012 | Tests — `RegisterVendorServiceUT` (5 casos, Mockito puro) + `RegisterVendorIT` (8 casos: 401/403/400/201, Nimbus JWT). | Sin dependencias nuevas — JWT firmado con Nimbus JOSE (ya en classpath via oauth2-resource-server). Pendiente: ejecutar y reportar salida. |
| 2026-04-24 | EPIC-01 / US-013 | Application layer — `RegisterClientCommand`, `RegisterClientResult`, `RegisterClientUseCase` (port in), `RegisterClientService` en `user/application`. | Orquesta: resuelve Vendor por UUID (ADR-02) → genera temp password → persiste User(CLIENT) + Client → registra CLIENT_REGISTRATION en notification_log. |
| 2026-04-24 | EPIC-01 / US-013 | REST adapter — `RegisterClientRequest` + `RegisterClientResponse` + `RegisterClientRequestMapper`. Endpoint `POST /api/v1/auth/clients` añadido a `AuthController`. | Endpoint PÚBLICO (permitAll en AuthSecurityConfig). Requiere `vendorUuid` para multi-tenancy. |
| 2026-04-24 | EPIC-01 / US-013 | Tests — `RegisterClientServiceUT` (5 casos) + `RegisterClientIT` (5 casos: 400/201 público). Updated `RegisterVendorIT` con `@MockBean RegisterClientUseCase`. | Pendiente: ejecutar y reportar salida. |
| 2026-04-24 | EPIC-01 / US-014 | **No requiere código backend.** Login gestionado por Supabase Auth (ADR-06/ADR-09). Backend solo valida JWT — ya implementado en `SupabaseJwtAuthConverter` + `SecurityConfig`. Redirección por rol es frontend. | Scope: frontend only. |
| 2026-04-24 | EPIC-01 / US-015 | Refactorización RBAC — 8 SecurityConfigs actualizadas: `hasRole("ADMIN")` → `hasAnyRole("VENDOR", "SUPER_ADMIN")`. Módulos: Account, Dashboard, Order, Profile, Service, Subscription, Reservation, Client. | Alineación con ADR-08 and `UserRole` enum. Zero referencias residuales a `ADMIN`. `ProfileSecurityConfig` cambió `@Component` → `@Configuration`. `ClientSecurityConfig` añadió restricción `GET /clients` (lista) a `VENDOR`+`SUPER_ADMIN`. |
| 2026-04-24 | EPIC-01 / US-016 | **No requiere código backend.** Backend stateless (`SessionCreationPolicy.STATELESS`). Sin sesión server-side. Invalidación es Supabase Auth (revoke token). Token local eliminado por browser. | Scope: frontend only. |
| 2026-04-27 | EPIC-02 / US-017 | `ServiceRequest` enriquecido con campos obligatorios: `category`, `priceProfile`, `priceFull`, `durationDays`, `maxProfiles` + opcionales `description`/`imageUrl`. `ServiceMapper` actualizado. `DigitalServiceService.create()` resuelve `vendorId` del JWT subject (externalId → User → Vendor). | `POST /api/v1/services` requiere `VENDOR`/`SUPER_ADMIN`. |
| 2026-04-27 | EPIC-02 / US-018 | `DigitalServiceService.update()` actualiza todos los campos editables. Verificación de ownership: JWT subject → `UserRepositoryPort.findByExternalId` → `VendorRepositoryPort.findByUserId` → compara `vendorId`. | Ownership fallido lanza `AccessDeniedException` → HTTP 403. |
| 2026-04-27 | EPIC-02 / US-019 | `PATCH /api/v1/services/{id}/status` — toggle `isActive`. Verifica ownership. No afecta suscripciones activas (solo el flag de visibilidad). | `ServiceSecurityConfig` actualizado con regla `PATCH`. |
| 2026-04-27 | EPIC-02 / US-020 | `GET /api/v1/services/vendor/{vendorUuid}?category=&isActive=` — panel del vendedor. `ServiceRepositoryPort` + `SpringDataServiceRepository` con `findAllByVendorId` y `@Query` con filtros opcionales. `ServiceUseCase.listByVendor()`. | Requiere `VENDOR`/`SUPER_ADMIN`. |
| 2026-04-27 | EPIC-02 / US-021 | `GET /api/v1/services/store/{vendorUuid}` — catálogo público, solo activos. `findActiveByVendorId` en port/adapter/repo. | Endpoint público (`permitAll`). |
| 2026-04-27 | EPIC-02 / Infra | `ServiceResponse` enriquecido. `ServiceSecurityConfig` reescrito con RBAC granular. `ServicePersistenceMapper` ya correcto (sin cambios). | |
| 2026-04-27 | EPIC-02 / Tests | `DigitalServiceServiceUT` (11 casos: US-017..021 incluyendo ownership 403). `ServiceControllerIT` (9 casos: RBAC 401/403/400/201, filtros, público). | Pendiente: ejecutar y reportar salida. |
| 2026-04-27 | Contract / US-012 | **Auth refactor (ADR-09 revised):** Elimina `temporaryPassword`. `RegisterVendorRequest` incluye `externalId` obligatorio (Supabase UUID). Response DTOs sin `temporaryPassword`/`manualStep`. | Frontend gestiona creación en Supabase; backend persiste vínculo 1:1. |
| 2026-04-27 | Contract / US-013 | Mismo refactor para clientes. `RegisterClientCommand` añade `externalId`. `RegisterClientService` usa el externalId provisto. | Igual que US-012. |
| 2026-04-27 | Multitenancy | `GET /api/v1/vendors/public/{uuid}` → `VendorPublicResponse(id, storeName, logoUrl)`. `VendorPublicController` + `VendorSecurityConfig` creados (`/public/**` = permitAll). | Storefront resuelve identidad del vendor dinámicamente. |
| 2026-04-27 | DTOs / Consistencia | `OrderResponse.uuid` → `id`. `ReservationResponse.uuid` → `id`. `ReservationDetailResponse.uuid` → `id`. Mappers actualizados. | Estandarización del campo identificador público en todos los módulos. |
| 2026-04-27 | DTOs / Lenguaje ubicuo | `ServiceRequest/Response.priceFull` → `priceComplete`. `ServiceMapper` + `ServiceControllerIT` actualizados. | Alineación con Glosario B.1: "Cuenta Completa". Domain model y columna DB no cambian. |
| 2026-04-27 | EPIC-03 / Fundación | V18__add_status_to_profiles.sql — ADD `status VARCHAR(20) NOT NULL DEFAULT 'available'` a tabla `profiles`. `ProfileStatus` ampliado: AVAILABLE, ACTIVE, RESERVED, OCCUPIED, BLOCKED, EXPIRED. `Profile` domain + `ProfileEntity` + `ProfilePersistenceMapper` + `ProfileResponse` + `ProfileMapper` actualizados. | ACTIVE/RESERVED/OCCUPIED/EXPIRED = solo sistema (EPIC-06/07). AVAILABLE/BLOCKED = manual (US-027). |
| 2026-04-27 | EPIC-03 / US-022 | `AccountRequest` enriquecido: `cost`, `source`, `purchasedAt`, `plan`. `AccountResponse` añade `status`, `saleMode`, `cost`, `source`, `purchasedAt` (password excluido). `AccountMapper` actualizado. `CreateAccountUseCase.create()` → `create(account, callerExternalId)`. `CreateAccountService` resuelve `vendorId` del JWT (ADR-09). `AccountController` extrae `externalId` del `JwtAuthenticationToken`. | `generateProfilesForAccount` ahora recibe `vendorId` para multi-tenancy. |
| 2026-04-27 | EPIC-03 / US-023 | `UpdateAccountUseCase` + `UpdateAccountService` — ownership check (callerVendorId == account.vendorId → 403). `PUT /api/v1/accounts/{id}`. Campos `id`/`uuid` inmutables (BR-US023-01). | |
| 2026-04-27 | EPIC-03 / US-024 | `AccountRepositoryPort` + `SpringDataAccountRepository` + `JpaAccountAdapter` extendidos con `findByVendorId` y `findByVendorIdFiltered(vendorId, serviceId?, status?)`. `ListAccountsUseCase` + `ListAccountsService` nuevos. `GET /api/v1/accounts/vendor/{vendorUuid}?serviceUuid=&status=`. | JPQL con filtros opcionales nullables. |
| 2026-04-27 | EPIC-03 / US-025 | `ProfileUseCase.generate(accountUuid, count, callerExternalId)` + `ProfileService.generate()` — ownership check + validación `existing + count <= service.maxProfiles`. `POST /api/v1/accounts/{id}/profiles/generate?count=N`. | |
| 2026-04-27 | EPIC-03 / US-026 | `ProfileUseCase.update(profileUuid, name, pin, isOwner, callerExternalId)` + `ProfileService.update()` — ownership via `profile → account.vendorId`. `PUT /api/v1/profiles/{id}` actualizado con JWT. | |
| 2026-04-27 | EPIC-03 / US-027 | `ChangeProfileStatusRequest` record. `ProfileUseCase.changeStatus()` + `ProfileService.changeStatus()` — valida que `newStatus ∈ {AVAILABLE, BLOCKED}`, rechaza 400 si sistema-controlado. `PATCH /api/v1/profiles/{id}/status`. | `ProfileSecurityConfig` ya cubre `/**` — no requirió cambio. |
| 2026-04-27 | EPIC-03 / US-028 | `AccountDetailResponse` + `ProfileSummaryResponse` + `AccountSummaryResponse` records. `GetAccountUseCase.getDetail()` + `GetAccountService.getDetail()` — carga profiles, agrupa por status, ownership check. `GET /api/v1/accounts/{id}/detail`. | Compilación limpia `./mvnw compile -q` → exit 0. Pendiente: ejecutar tests. |
