services:
  panel:
    build:
      context: ../..
      dockerfile: apps/panel/Dockerfile
    restart: unless-stopped
    environment:
      PANEL_API_URL: ${PANEL_API_URL}
      PANEL_SUPABASE_URL: ${PANEL_SUPABASE_URL}
      PANEL_SUPABASE_KEY: ${PANEL_SUPABASE_KEY}
      GRAFANA_URL: ${GRAFANA_URL}
    expose:
      - "80"

