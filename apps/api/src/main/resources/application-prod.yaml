spring:
  datasource:
    # Session Pooler (IPv4-compatible). Get values from Supabase → Settings → Database → Session Pooler.
    # URL format:     jdbc:postgresql://aws-0-REGION.pooler.supabase.com:5432/postgres?preparedStatementCacheQueries=0&preparedStatementCacheSizeMiB=0
    # Username format: postgres.PROJECTREF  (NOT just "postgres")
    # Password:        your Supabase DB password
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 300000
      connection-timeout: 20000
      max-lifetime: 1800000
      keepalive-time: 60000
      validation-timeout: 3000
      pool-name: panel-db-pool
      data-source-properties:
        stringtype: unspecified

  flyway:
    enabled: true
    execute-in-transaction: false
    baseline-on-migrate: true
    baseline-version: 0
    validate-on-migrate: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: false

  lifecycle:
    timeout-per-shutdown-phase: 30s

server:
  port: ${SERVER_PORT:8080}
  forward-headers-strategy: native
  shutdown: graceful

logging:
  level:
    root: INFO
  structured:
    format:
      console: ecs

supabase:
  jwt:
    secret: ${SUPABASE_JWT_SECRET}
  url: ${SUPABASE_URL}
  service-role-key: ${SUPABASE_SERVICE_ROLE_KEY}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS}

neversion:
  renewal:
    grace-period-days: ${NEVERSION_RENEWAL_GRACE_PERIOD_DAYS:2}
  cron:
    subscription-expiry:
      enabled: ${NEVERSION_CRON_SUBSCRIPTION_EXPIRY_ENABLED:true}
      schedule: ${NEVERSION_CRON_SUBSCRIPTION_EXPIRY_SCHEDULE:0 0 8 * * *}
    renewal-reminders:
      enabled: ${NEVERSION_CRON_RENEWAL_REMINDERS_ENABLED:true}
  notification-service:
    host: ${NEVERSION_NOTIFICATION_SERVICE_HOST:notification-service}
    port: ${NEVERSION_NOTIFICATION_SERVICE_PORT:50051}


springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
