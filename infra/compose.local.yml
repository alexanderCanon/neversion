services:
  db:
    image: postgres:17
    container_name: neversion-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-neversiondb}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mysecretpassword}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-neversiondb}" ]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - neversion-network

#  # ── GoTrue — Supabase Auth Local ────────────────────────────────
#  gotrue:
#    image: supabase/gotrue:v2.158.0
#    container_name: neversion-gotrue
#    restart: unless-stopped
#    depends_on:
#      db:
#        condition: service_healthy
#    ports:
#      - "9999:8081"
#    environment:
#      API_EXTERNAL_URL: http://localhost:9999
#      GOTRUE_API_HOST: 0.0.0.0
#      GOTRUE_PORT: 8081
#      GOTRUE_API_EXTERNAL_URL: http://localhost:9999
#      GOTRUE_DB_DRIVER: postgres
#      GOTRUE_DB_DATABASE_URL: postgres://postgres:mysecretpassword@db:5432/neversiondb?sslmode=disable&search_path=auth,public
#      GOTRUE_JWT_SECRET: ${AUTH_JWT_SECRET:-your-dev-jwt-secret}
#      GOTRUE_JWT_EXP: 3600
#      GOTRUE_JWT_AUD: authenticated
#      GOTRUE_SITE_URL: http://localhost:4200
#      GOTRUE_DISABLE_SIGNUP: "false"
#      GOTRUE_MAILER_AUTOCONFIRM: "true"
#      GOTRUE_EXTERNAL_EMAIL_ENABLED: "true"
#      GOTRUE_SMTP_ADMIN_EMAIL: noreply@neversion.com
#      GOTRUE_SMTP_HOST: smtp.example.com
#      GOTRUE_SMTP_PORT: 587
#      GOTRUE_SMTP_USER: smtp_user
#      GOTRUE_SMTP_PASS: smtp_password
volumes:
  pgdata:

networks:
  neversion-network:
    external: true